# 🔤 StringDemo

A comprehensive **Java console application** demonstrating **String**, **StringBuffer**, and **StringBuilder** methods with **practical API scenarios** and **real-world use cases**.

## 🚀 Features Demonstrated
- ✅ **String Methods** - 18+ core methods (length, substring, replace, split, etc.)
- ✅ **StringBuffer** - Thread-safe mutable strings with `lastIndexOf()`, `substring()`
- ✅ **StringBuilder** - Single-threaded performance optimized strings
- ✅ **API Response Building** - JSON-like response construction
- ✅ **Data Parsing** - CSV parsing, email validation, URL checking

  ## 🎯 Java String Concepts Demonstrated

- **String Immutability**  
  `String` **immutable** vs mutable `StringBuffer/StringBuilder` - memory efficiency

- **Performance**  
  **StringBuilder** (single-thread, faster) vs **StringBuffer** (multi-thread, synchronized)

- **Method Chaining**  
  `append().append().toString()` - **fluent API** building

- **Parsing**  
  `split()`, `substring()`, `indexOf()` for **data extraction** from strings

- **Validation**  
  `endsWith()`, `startsWith()`, `contains()` - **input verification**

- **Real-world**  
  **API response building**, CSV parsing, email validation scenarios


## 🧩 Class Structure
```text
StringMethodsDemo
├── Basic operations (length, charAt, substring)
├── Search (indexOf, contains, startsWith)
├── Manipulation (replace, trim, toUpperCase)
└── Validation (email, URL, CSV parsing)

StringBufferDemo → Thread-safe API building
StringBuilderDemo → Single-threaded API building
```

## 🛠️ Tech Stack
- **Java 8+**
- **String API** (immutable)
- **StringBuffer** (synchronized)
- **StringBuilder** (unsynchronized)

## 📥 Setup & Run
```bash
git clone https://github.com/CoreJavaPulse/StringDemo.git
cd Java StringDemo
javac *.java
java StringMethodsDemo
java StringBufferDemo
java StringBuilderDemo
```
## 👨‍💻 Author
**Hitesh Mane**  
Java Full Stack Developer | Pune, India  

📧 **Email**: [hiteshmane5hm@gmail.com](mailto:hiteshmane5hm@gmail.com)  
💻 **GitHub**: [CoreJavaPulse](https://github.com/CoreJavaPulse)  
📍 **Location**: Pune, Maharashtra  

